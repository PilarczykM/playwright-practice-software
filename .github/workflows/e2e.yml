name: E2E Tests
on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: .github/actions/setup
      - uses: .github/actions/test
